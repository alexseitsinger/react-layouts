import React from "react"
import { matchers } from "jest-emotion"

expect.extend(matchers)

describe("FixedHeader", () => {
  it("should render with initial height then update to fixed height", () => {
    console.log("test")
  })
})
